from django.urls import path
from customer import views

app_name = "customer"
urlpatterns = [
    # path("order_history/", views.OrderHistory.as_view(), name="order-history"),
    # path("order_status/", views.OrderHistory.as_view(), name="order-status"),
    # path("address/", views.OrderHistory.as_view(), name="address"),
    # path("profile/", views.OrderHistory.as_view(), name="profile"),
    # path("profile/update/", views.OrderHistory.as_view(), name="profile-update"),
]
